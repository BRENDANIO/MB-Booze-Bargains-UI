# MB-Booze-Bargains-UI
The UI component that works in tandem with the backend server. This displays the relevant information that's held in the database, allowing the user to traverse the various sales.
